_schema-version: "3.2"
ID: frontendui02
version: 0.0.1
modules:
- name: frontendui02-approuter
  type: approuter.nodejs
  path: frontendui02-approuter
  requires:
  - name: frontendui02_html_repo_runtime
  - name: uaa_frontendui02
  - name: frontendui02-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  properties:
    COOKIE_BACKWARD_COMPATIBILITY: true
    COOKIES: {
      "SameSite" : "None"
    }
  parameters:
    disk-quota: 256M
    memory: 256M
- name: frontendui02_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: frontendui02_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - frontendui02.zip
      name: frontendui02
      target-path: resources/
- name: frontendui02
  type: html5
  path: frontendui02
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: frontendui02_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_frontendui02
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: frontendui02-xsuaa-service
    service-plan: application
- name: frontendui02_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: frontendui02-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: frontendui02-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
